import React from "react";
import classes from "./Featured.module.scss";

const Featured = () => {
  return (
    <section className={`${classes.featured} section`}>
      <h2>Featured</h2>
    </section>
  );
};

export default Featured;
