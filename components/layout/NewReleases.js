import React from "react";
import classes from "./NewReleases.module.scss";

const NewReleases = () => {
  return (
    <section className={`section ${classes.newReleases}`}>
      <h2>New Releases</h2>
    </section>
  );
};

export default NewReleases;
